header(
    style={
        background: "url(/index/slide.jpg) no-repeat",
        display: "flow-root",
        "background-size": "100%"
    })
    img(
        src="index/logo2.png"
        width="75px"
        height="75px"
        style={
            margin: "10px",
            float: "left",
        }
    )
    img(
        src=user_avatar
        style={
            "border-radius": "50%",
            margin: "10px",
            float: "right",
            border: "4px solid #fdd835"
        }
        width="71px"
        height="71px"
    )
    img(
        id="open-button"
        class="open-button"
        src="/index/gabe"
        width="100px"
        height="80px"
        style={
            float: "right",
            margin: "10px 20px",
            cursor: "pointer",
            animation: "pulse 1.25s infinite cubic-bezier(0.66, 0, 0, 1), shake 0.5s infinite"
        }
    )
    ul(class="topnav" id="myTopnav")
        li
            a(class="active" href="#home") Курсы
        li
            a(href="#news") Песочница
        li
            a(href="#contact") Форум
        li
            a(href="#about") Играть
        li
            a(href="#about") Топ
style.
    .topnav {
        list-style-type: none;
        margin: 0;
        padding: 0;
        overflow: hidden;
    }

    .topnav > li > a {
        float: left;
    }

    .topnav > li > a {
        display: inline-block;
        color: #f2f2f2;
        text-align: center;
        padding: 26px;
        text-decoration: none;
        font-size: 42px;
    }

    .topnav > li > a:hover {
        color: #fff;
    }
    @keyframes shake {
        0% {
            transform: translate(1px, 1px) rotate(0deg);
        }
        10% {
            transform: translate(-1px, -2px) rotate(-1deg);
        }
        20% {
            transform: translate(-3px, 0px) rotate(1deg);
        }
        30% {
            transform: translate(3px, 2px) rotate(0deg);
        }
        40% {
            transform: translate(1px, -1px) rotate(1deg);
        }
        50% {
            transform: translate(-1px, 2px) rotate(-1deg);
        }
        60% {
            transform: translate(-3px, 1px) rotate(0deg);
        }
        70% {
            transform: translate(3px, 1px) rotate(-1deg);
        }
        80% {
            transform: translate(-1px, -1px) rotate(1deg);
        }
        90% {
            transform: translate(1px, 2px) rotate(0deg);
        }
        100% {
            transform: translate(1px, -2px) rotate(-1deg);
        }
    }